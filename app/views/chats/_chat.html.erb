<div id="<%= dom_id chat %>">
  <p>
    <strong><%= link_to "#{chat.title.blank? ? "<>" : chat.title}", chat %>:</strong>
  </p>

  <div class='row'>
    <%= display_message_for(chat) %>
  </div>
</div>
