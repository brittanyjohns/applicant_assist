<%= form_with url: application_chats_url(application), id: "form" do |f| %>
  <div class="form-inputs">
    <%= f.label :title %>
    <%= f.text_field :title %>
  </div>
  <div class="form-actions">
    <%= f.button :submit %>
  </div>
<% end %>
