


  <div class="col">
  <%= form_with(model: chat, url: message_prompt_chat_path(chat), method: :post) do |form| %>
    <div class="btn-group-vertical float-start img-type-btn-group" role="group" aria-label="Vertical button group">
    <%= form_links form, @chat.message_types %>
  </div> 
  <% end %>
</div>
