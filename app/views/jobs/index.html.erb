<p style="color: green"><%= notice %></p>

<h1>Jobs</h1>

<div id="jobs">
  <% @jobs.each do |job| %>
  <p class='lead'>
    <%= job.title %>
  </p>
  <p>
    <%= link_to job.title, job %>
  </p>
  <% end %>
</div>

<%= link_to "New job", new_job_path %>
